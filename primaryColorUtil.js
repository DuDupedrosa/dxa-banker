let primaryColor = '#ff0000'; // Defina a cor primária padrão (vermelho)

export const setPrimaryColor = (color) => {
  primaryColor = color;
};

export const getPrimaryColor = () => {
  return primaryColor;
};
